sku没有标准，很多size后面跟原本应该是备注的字符串，size中间也可能缺失符号，导致只能模糊匹配出很多带后缀的sku，也没有办法实现选中最精简的size进行子串匹配
有一个方案，统计一个sku有哪些item code，并一对一输出到屏幕上。再进行搜索



sku目前是子串匹配，会有问题，需要改成开头完全匹配，sku选择项可以尝试改成excel的filter那种
prefect


考虑将合并表输出改为由主表匹配的item code匹配到的附加表，另外显示匹配到的结果
原因，重复合并太多了。如果另外显示，第一，后端处理会快很多，第二处理后显示的结果会更少
如果合并不可避免，或许可以在导出excel阶段，合并

现在将sku筛选框设计为excel filter了，但是由于只选择了20条，导致没有全部显示。
当我选择一项时，如果字符长度过长，前端渲染会一直适应长度导致页面闪烁。
将接受的选项放开到30条。前端渲染问题给出解释和解决方案
